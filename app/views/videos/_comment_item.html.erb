<div class="comment-item">
	<div class="comment-username">
		<%= link_to comment_item.user.username, comment_item.user %>
		<p><%= comment_item.content %></p>
	</div>
	<div class="comment-time">
		<div><%= time_ago_in_words(comment_item.created_at) %> ago</div>
		<% if current_user?(comment_item.user) %>
			<%= link_to "Delete", comment_item, :method => :delete, :class => "comment-delete" %>
		<% end %>
	</div>
</div>