<div class="left-container">
	<div class="video-feed-header"><%= @header_title %></div>
		<div id="results">
			<%= render 'shared/feed' %>
			<%= will_paginate @feed_items %>
			<%= pageless(@feed_items.total_pages, @path, "#results") %>
		</div>
</div>